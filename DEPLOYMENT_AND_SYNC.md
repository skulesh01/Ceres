# 🚀 CERES: Развертывание и синхронизация с GitHub

## Обзор

Набор скриптов для автоматического развертывания Ceres на сервер и синхронизации изменений с GitHub.

## 📋 Скрипты

### 1. `deploy-to-server.ps1` - Развертывание на сервер

Развертывает Ceres на удаленный сервер через Ceres-Private.

**Использование:**
```powershell
# Полное развертывание
.\scripts\deploy-to-server.ps1 -Mode deploy

# Тест подключения
.\scripts\deploy-to-server.ps1 -Mode test

# Проверка статуса
.\scripts\deploy-to-server.ps1 -Mode status
```

**Что делает:**
- Проверяет наличие Ceres-Private с credentials.json
- Запускает deploy-to-proxmox.py из Ceres-Private
- Выполняет полное развертывание на Proxmox сервер

### 2. `git-auto-push.ps1` - Автоматический пуш на GitHub

Коммитит изменения и пушит их на GitHub.

**Использование:**
```powershell
# Автоматический коммит с автоматически сгенерированным сообщением
.\scripts\git-auto-push.ps1 -AutoCommit

# Коммит с указанным сообщением
.\scripts\git-auto-push.ps1 -Message "feat: оптимизация скриптов развертывания" -AutoCommit

# Интерактивный режим (с подтверждением)
.\scripts\git-auto-push.ps1 -Message "refactor: улучшение производительности"
```

**Что делает:**
- Проверяет изменения в репозитории
- Автоматически определяет тип изменений и генерирует сообщение коммита
- Коммитит изменения
- Пушит на GitHub

**Автоматические сообщения коммитов:**
- Изменения в скриптах (`.ps1`, `.sh`, `.py`) → `refactor: оптимизация скриптов развертывания`
- Изменения в конфигах (`config/`) → `config: обновление конфигураций`
- Изменения в документации (`.md`, `.txt`) → `docs: обновление документации`
- Остальное → `chore: обновления и улучшения проекта`

### 3. `deploy-and-sync.ps1` - Комплексный скрипт

Выполняет развертывание и синхронизацию с GitHub одним вызовом.

**Использование:**
```powershell
# Развертывание + пуш на GitHub
.\scripts\deploy-and-sync.ps1 -DeployMode deploy -PushChanges

# Только тест подключения + пуш изменений
.\scripts\deploy-and-sync.ps1 -DeployMode test -PushChanges

# Только пуш изменений (без развертывания)
.\scripts\deploy-and-sync.ps1 -DeployMode skip -PushChanges -CommitMessage "fix: исправление бага"

# Только развертывание (без пуша)
.\scripts\deploy-and-sync.ps1 -DeployMode deploy
```

## 📝 Примеры использования

### Пример 1: Развертывание после оптимизации

```powershell
# Вы сделали изменения в скриптах для оптимизации
# Развернуть на сервер + запушить на GitHub
.\scripts\deploy-and-sync.ps1 -DeployMode deploy -PushChanges
```

### Пример 2: Регулярный пуш улучшений

```powershell
# После любых улучшений в проекте
.\scripts\git-auto-push.ps1 -AutoCommit
```

### Пример 3: Тест развертывания перед коммитом

```powershell
# Сначала проверить подключение
.\scripts\deploy-to-server.ps1 -Mode test

# Если всё ОК, развернуть
.\scripts\deploy-to-server.ps1 -Mode deploy

# И запушить изменения
.\scripts\git-auto-push.ps1 -AutoCommit
```

## ⚙️ Предварительные требования

### Для развертывания:

1. **Ceres-Private** должен быть настроен:
   - Папка `Ceres-Private/` рядом с `Ceres/`
   - Файл `Ceres-Private/credentials.json` с реальными паролями
   - Скрипты `launcher.py` и `deploy-to-proxmox.py` в Ceres-Private

2. **Python 3.7+** установлен

3. **SSH доступ** к Proxmox серверу настроен

### Для GitHub синхронизации:

1. **Git репозиторий** инициализирован в папке Ceres
   ```powershell
   cd Ceres
   git init
   git remote add origin https://github.com/YOUR_USERNAME/Ceres.git
   ```

2. **GitHub credentials** настроены
   ```powershell
   git config --global credential.helper wincred
   ```

3. **Права доступа** на GitHub репозиторий

## 🔐 Безопасность

⚠️ **ВАЖНО:**
- `Ceres-Private/` НЕ должен попасть в Git (уже в `.gitignore`)
- `credentials.json` НЕ коммитится
- Все пароли хранятся только в Ceres-Private

## 📊 Типичный workflow

### Для разработки и оптимизации:

```powershell
# 1. Вы внесли изменения/оптимизации в код

# 2. Развернуть на сервер + запушить на GitHub
.\scripts\deploy-and-sync.ps1 -DeployMode deploy -PushChanges

# Или отдельно:
.\scripts\deploy-to-server.ps1 -Mode deploy
.\scripts\git-auto-push.ps1 -AutoCommit
```

### Для быстрого обновления только на GitHub:

```powershell
# Только пуш изменений без развертывания
.\scripts\git-auto-push.ps1 -AutoCommit
```

## 🐛 Решение проблем

### Ошибка: "Ceres-Private not found"

**Решение:** Убедитесь что папка `Ceres-Private/` находится рядом с `Ceres/`:
```
All_project/
├── Ceres/
└── Ceres-Private/  ← должна быть здесь
```

### Ошибка: "No remote 'origin' configured"

**Решение:** Настройте GitHub remote:
```powershell
cd Ceres
git remote add origin https://github.com/YOUR_USERNAME/Ceres.git
```

### Ошибка: "Push failed"

**Решение:** Проверьте:
1. GitHub credentials настроены
2. Права доступа к репозиторию
3. Сетевое подключение
4. Попробуйте настроить credential helper:
   ```powershell
   git config --global credential.helper wincred
   ```

### Ошибка: "Python not found"

**Решение:** Установите Python 3.7+ и добавьте в PATH, или используйте `python3` вместо `python`.

## 📚 Связанные документы

- [README.md](README.md) - Общая информация о проекте
- [DEPLOYMENT_STEPS.md](DEPLOYMENT_STEPS.md) - Детальное руководство по развертыванию
- [Ceres-Private/README.md](../Ceres-Private/README.md) - Документация по приватной части

## ✅ Чеклист перед использованием

- [ ] Ceres-Private настроен с credentials.json
- [ ] Git репозиторий инициализирован
- [ ] GitHub remote настроен (`git remote add origin ...`)
- [ ] GitHub credentials настроены
- [ ] Python установлен и в PATH
- [ ] SSH доступ к серверу работает

## 🎯 Рекомендации

1. **Регулярные пуши:** После любых оптимизаций и улучшений запускайте `git-auto-push.ps1`
2. **Тестирование:** Перед развертыванием используйте `-Mode test` для проверки подключения
3. **Коммит сообщения:** Используйте осмысленные сообщения для лучшей истории проекта
4. **Безопасность:** Никогда не коммитьте пароли и credentials

---

**Создано для автоматизации развертывания и синхронизации CERES проекта** 🚀
