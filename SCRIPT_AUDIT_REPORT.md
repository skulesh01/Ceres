# SCRIPT AUDIT REPORT - CERES Project

**Дата**: 18 января 2026  
**Аудитор**: GitHub Copilot (Claude Sonnet 4.5)  
**Цель**: Определить необходимые скрипты и интегрировать их функционал в единый CLI (ceres.ps1)

---

## 📊 ОБЗОР

**Всего скриптов**: 119  
- PowerShell (.ps1): 66  
- Bash (.sh): 53

**Проблема**: Разрозненные скрипты затрудняют поддержку и использование проекта.

**Решение**: Интеграция всех нужных функций в единый CLI `ceres.ps1` с модульной архитектурой.

---

## 🎯 КАТЕГОРИЗАЦИЯ СКРИПТОВ

### ✅ КАТЕГОРИЯ 1: ИНТЕГРИРОВАТЬ В ceres.ps1 (Высокий приоритет)

Эти скрипты реализуют важную функциональность и должны стать командами CLI.

#### 1.1 Docker/Compose Management
| Скрипт | Функционал | Новая команда | Статус |
|--------|-----------|---------------|---------|
| `scripts/start.ps1` | Запуск Docker Compose | `ceres start [modules]` | ✅ НУЖЕН |
| `scripts/status.ps1` | Проверка статуса сервисов | `ceres status [-Detailed]` | ✅ НУЖЕН |
| `scripts/cleanup.ps1` | Остановка и очистка | `ceres stop / ceres clean` | ✅ НУЖЕН |
| `scripts/backup.ps1` | Бэкап volumes/БД | `ceres backup` | ✅ НУЖЕН |
| `scripts/restore.ps1` | Восстановление | `ceres restore <timestamp>` | ✅ НУЖЕН |

#### 1.2 Configuration & Setup
| Скрипт | Функционал | Новая команда | Статус |
|--------|-----------|---------------|---------|
| `scripts/configure-ceres.ps1` | Интерактивная конфигурация | `ceres configure [profile]` | 🔄 ЧАСТИЧНО (есть в TODO) |
| `scripts/analyze-resources.ps1` | Анализ ресурсов системы | `ceres analyze resources` | ✅ УЖЕ РЕАЛИЗОВАН |
| `scripts/keycloak-bootstrap.ps1` | Настройка Keycloak OIDC | `ceres setup keycloak` | ✅ НУЖЕН |
| `scripts/keycloak-smtp.ps1` | SMTP для Keycloak | `ceres setup smtp` | ✅ НУЖЕН |

#### 1.3 User Management
| Скрипт | Функционал | Новая команда | Статус |
|--------|-----------|---------------|---------|
| `scripts/create-employee.ps1` | Создание сотрудника (mail+VPN) | `ceres user create <username>` | ✅ НУЖЕН |
| `scripts/add-vpn-user.ps1` | Добавить VPN клиента | `ceres vpn add <username>` | ✅ НУЖЕН |

#### 1.4 Validation
| Скрипт | Функционал | Новая команда | Статус |
|--------|-----------|---------------|---------|
| `scripts/preflight.ps1` | Предварительные проверки | `ceres validate preflight` | ✅ НУЖЕН |
| `config/validate-deployment.ps1` | Проверка деплоя | `ceres validate deployment` | ✅ НУЖЕН |

---

### 🔧 КАТЕГОРИЯ 2: KUBERNETES/FLUX (Специализированные)

Эти скрипты для Kubernetes оставить как есть, но добавить обёртки в ceres.ps1.

| Скрипт | Функционал | Решение |
|--------|-----------|---------|
| `DEPLOY.ps1` | Автодеплой k3s на Proxmox | 🟡 Обёртка: `ceres k8s deploy` |
| `flux-status.ps1` | Проверка Flux GitOps | 🟡 Обёртка: `ceres k8s flux-status` |
| `flux/bootstrap.ps1` | Bootstrap FluxCD | 🟡 Обёртка: `ceres k8s flux-bootstrap` |
| `scripts/Deploy-Kubernetes.ps1` | Деплой на K8s | 🟡 Обёртка: `ceres k8s deploy-stack` |

**Решение**: Создать модуль `scripts/_lib/Kubernetes.ps1` с функциями-обёртками.

---

### ❌ КАТЕГОРИЯ 3: УДАЛИТЬ (Legacy/Duplicate)

Эти скрипты устарели, дублируют функционал или не нужны.

#### 3.1 Legacy Scripts (Устаревшие)
| Скрипт | Причина удаления |
|--------|-----------------|
| `scripts/Check-System.ps1` | Дублирует `ceres analyze resources` |
| `scripts/test-cli.ps1` | Тестовый скрипт для разработки |
| `scripts/test-analyze.ps1` | Тестовый скрипт для разработки |
| `scripts/test-profiles.ps1` | Тестовый скрипт |
| `scripts/Test-Installation.ps1` | Старый тест |
| `test-direct.ps1` | Тестовый файл в корне |

#### 3.2 Duplicate/Old Deployment
| Скрипт | Причина удаления |
|--------|-----------------|
| `scripts/deploy-quick.ps1` | Дублирует `DEPLOY.ps1` |
| `scripts/full-setup.ps1` | Дублирует `start.ps1` |
| `scripts/full-auto-setup.ps1` | Дублирует `start.ps1` |
| `scripts/auto-deploy-ceres.ps1` | Дублирует `DEPLOY.ps1` |
| `scripts/deploy-full-stack.ps1` | Слишком специфичен, использовать `start.ps1` |
| `auto-deploy.ps1` (корень) | Дублирует `DEPLOY.ps1` |
| `check-status.ps1` (корень) | Дублирует `status.ps1` |

#### 3.3 Config Directory Scripts
Эти скрипты в `config/` лучше переместить или удалить:

| Скрипт | Причина |
|--------|---------|
| `config/test-services.ps1` | Дублирует `status.ps1` |
| `config/включить-все-сервисы.ps1` | Дублирует `start.ps1` |
| `config/validate-deployment.ps1` | Интегрировать в `ceres validate` |

#### 3.4 Shell Scripts Duplicates
| Скрипт | Причина |
|--------|---------|
| `scripts/deploy.sh` | Дублирует PowerShell версию |
| `scripts/cleanup.sh` | Дублирует `cleanup.ps1` |
| `config/test-services.sh` | Дублирует PS1 версию |
| `config/enable-all-services.sh` | Дублирует `start.ps1` |

---

### 🟡 КАТЕГОРИЯ 4: СОХРАНИТЬ (Специализированные)

Эти скрипты решают узкие задачи, их оставить как есть.

#### 4.1 Export/Certificates
| Скрипт | Причина |
|--------|---------|
| `scripts/export-caddy-rootca.ps1` | Экспорт CA сертификата Caddy |
| `scripts/generate-mtls-certs.ps1` | Генерация mTLS сертификатов |
| `scripts/generate-mtls-certs.sh` | Bash версия |

#### 4.2 Advanced Operations
| Скрипт | Причина |
|--------|---------|
| `scripts/setup-ha.sh` | HA кластер (advanced) |
| `scripts/setup-multi-cluster.sh` | Multi-cluster (advanced) |
| `scripts/monitor-ha-health.sh` | HA мониторинг |
| `scripts/cost-optimization.sh` | Оптимизация затрат |
| `scripts/instrument-services.sh` | Инструментация OpenTelemetry |

#### 4.3 GitHub Actions
| Скрипт | Причина |
|--------|---------|
| `scripts/add-github-secrets.ps1` | Настройка GitHub Secrets |
| `scripts/setup-github-secrets.ps1` | Дублирует, но специфичен для CI |
| `scripts/gh-ops/gh-actions.ps1` | Операции GitHub Actions |

#### 4.4 Remote Operations
| Скрипт | Причина |
|--------|---------|
| `scripts/remote-ops/remote.ps1` | Удалённые операции по SSH |
| `scripts/remote-ops/remote.sh` | Bash версия |

#### 4.5 Deployment Operators
| Скрипт | Причина |
|--------|---------|
| `scripts/deploy-operators.sh` | Деплой K8s операторов |
| `scripts/deploy-argocd.sh` | ArgoCD деплой |
| `scripts/deploy-kubernetes.sh` | K8s деплой |
| `scripts/deploy-3vm-enterprise.sh` | 3-VM enterprise setup |

---

### 📦 КАТЕГОРИЯ 5: АРХИВИРОВАТЬ

Старые скрипты, уже в archive/:

| Скрипт | Действие |
|--------|----------|
| `archive/scripts/test-wg-api.ps1` | ✅ Уже в архиве |
| `archive/scripts/test-vpn.ps1` | ✅ Уже в архиве |
| `archive/scripts/setup-wireguard.ps1` | ✅ Уже в архиве |
| `archive/scripts/MENU.ps1` | ✅ Уже в архиве |
| `archive/scripts/manage-wg-users.ps1` | ✅ Уже в архиве |

---

## 🎯 ПЛАН ДЕЙСТВИЙ

### ЭТАП 1: Интеграция основного функционала в ceres.ps1

#### 1.1 Создать новые команды (приоритет):
```powershell
ceres start [modules]           # из start.ps1
ceres stop                      # из cleanup.ps1
ceres status [-Detailed]        # из status.ps1
ceres backup                    # из backup.ps1
ceres restore <timestamp>       # из restore.ps1
ceres configure [profile]       # из configure-ceres.ps1
ceres validate preflight        # из preflight.ps1
ceres validate deployment       # из validate-deployment.ps1
ceres setup keycloak            # из keycloak-bootstrap.ps1
ceres setup smtp                # из keycloak-smtp.ps1
ceres user create <username>    # из create-employee.ps1
ceres vpn add <username>        # из add-vpn-user.ps1
```

#### 1.2 Создать модули для разных категорий:
```
scripts/_lib/
  ├── Docker.ps1       (start, stop, status, backup, restore)
  ├── Configure.ps1    (configure, validate)
  ├── Keycloak.ps1     (bootstrap, smtp)
  ├── User.ps1         (create employee, vpn)
  └── Kubernetes.ps1   (k8s команды)
```

#### 1.3 Обновить ceres.ps1:
```powershell
# Добавить новые группы команд
switch ($Command) {
    'start'     { Invoke-Start @args }
    'stop'      { Invoke-Stop @args }
    'status'    { Invoke-Status @args }
    'backup'    { Invoke-Backup @args }
    'restore'   { Invoke-Restore @args }
    'configure' { Invoke-Configure @args }
    'validate'  { Invoke-Validate @args }
    'setup'     { Invoke-Setup @args }
    'user'      { Invoke-User @args }
    'vpn'       { Invoke-Vpn @args }
    'k8s'       { Invoke-Kubernetes @args }
    # ...
}
```

### ЭТАП 2: Удаление дублирующих скриптов

**К удалению (41 файл):**

```powershell
# Корневые дубликаты
Remove-Item auto-deploy.ps1
Remove-Item check-status.ps1
Remove-Item test-direct.ps1

# Legacy/Test скрипты
Remove-Item scripts/Check-System.ps1
Remove-Item scripts/test-*.ps1
Remove-Item scripts/Test-Installation.ps1

# Дублирующие deployment
Remove-Item scripts/deploy-quick.ps1
Remove-Item scripts/full-setup.ps1
Remove-Item scripts/full-auto-setup.ps1
Remove-Item scripts/auto-deploy-ceres.ps1
Remove-Item scripts/deploy-full-stack.ps1

# Config дубликаты
Remove-Item config/test-services.*
Remove-Item config/включить-все-сервисы.ps1
Move-Item config/validate-deployment.ps1 scripts/_lib/

# Shell дубликаты
Remove-Item scripts/deploy.sh
Remove-Item scripts/cleanup.sh
Remove-Item config/enable-all-services.sh
```

### ЭТАП 3: Реорганизация структуры

**Новая структура:**

```
scripts/
├── ceres.ps1               # Главный CLI (обновлённый)
├── _lib/
│   ├── Common.ps1          # ✅ Существует
│   ├── Platform.ps1        # ✅ Существует
│   ├── Analyze.ps1         # ✅ Существует
│   ├── Validate.ps1        # ✅ Существует
│   ├── Docker.ps1          # 🆕 Создать
│   ├── Configure.ps1       # 🆕 Создать
│   ├── Keycloak.ps1        # 🆕 Создать
│   ├── User.ps1            # 🆕 Создать
│   └── Kubernetes.ps1      # 🆕 Создать
├── advanced/               # 🆕 Специализированные
│   ├── export-caddy-rootca.ps1
│   ├── generate-mtls-certs.ps1
│   ├── setup-ha.sh
│   ├── setup-multi-cluster.sh
│   └── cost-optimization.sh
└── ci/                     # 🆕 CI/CD специфичные
    ├── add-github-secrets.ps1
    ├── setup-github-secrets.ps1
    └── gh-ops/
```

### ЭТАП 4: Документация

**Обновить:**
- `README.md` — новые команды ceres.ps1
- `docs/00-QUICKSTART.md` — примеры использования
- Создать `docs/03-CLI_REFERENCE.md` — полная документация команд

---

## 📈 МЕТРИКИ

### До оптимизации:
- Скриптов: 119
- Дублирующих: ~35
- В корне: 4
- Сложность поддержки: ВЫСОКАЯ

### После оптимизации:
- Скриптов: ~50-60
- Единый CLI: ceres.ps1 (с 20+ командами)
- Модулей: 9
- Сложность поддержки: НИЗКАЯ

### Выгоды:
- ✅ Единая точка входа для пользователей
- ✅ Лучшая поддерживаемость кода
- ✅ Кроссплатформенность (Windows/Linux/macOS)
- ✅ Консистентный UX
- ✅ Меньше дублирования
- ✅ Проще тестирование

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

### Приоритет 1 (Критический):
1. Создать `scripts/_lib/Docker.ps1` (start, stop, status, backup, restore)
2. Обновить `ceres.ps1` с новыми командами
3. Удалить очевидные дубликаты (test-*, Check-System.ps1)

### Приоритет 2 (Высокий):
4. Создать `scripts/_lib/Configure.ps1` и `scripts/_lib/Keycloak.ps1`
5. Создать `scripts/_lib/User.ps1` (employee + VPN)
6. Переместить advanced скрипты в `scripts/advanced/`

### Приоритет 3 (Средний):
7. Создать `scripts/_lib/Kubernetes.ps1` (обёртки для k8s команд)
8. Удалить bash дубликаты (если есть PS версии)
9. Обновить документацию

### Приоритет 4 (Низкий):
10. Создать `docs/03-CLI_REFERENCE.md`
11. Добавить bash-completion для ceres
12. Создать интеграционные тесты

---

## ❓ ВОПРОСЫ ДЛЯ РЕШЕНИЯ

1. **Bash скрипты**: Оставить для Linux-only функционала или удалить все дубликаты?
   - **Рекомендация**: Удалить дубликаты, оставить только уникальные (Linux-специфичные).

2. **Kubernetes скрипты**: Интегрировать в CLI или оставить отдельными?
   - **Рекомендация**: Создать `ceres k8s <command>` обёртки.

3. **Config директория**: Скрипты в config/ — переместить в scripts/ или удалить?
   - **Рекомендация**: Переместить нужные в `scripts/_lib/`, остальное удалить.

4. **DEPLOY.ps1**: Оставить в корне или переместить?
   - **Рекомендация**: Оставить в корне (точка входа для k8s деплоя).

---

## ✅ ВЫВОДЫ

**Текущая ситуация**: 119 скриптов с большим дублированием и сложной структурой.

**Целевая ситуация**: ~50-60 скриптов, единый CLI `ceres.ps1` с 20+ командами, модульная архитектура.

**Преимущества унификации**:
- Упрощение использования проекта (один CLI вместо десятков скриптов)
- Лучшая поддерживаемость (модули вместо разрозненных файлов)
- Кроссплатформенность (PowerShell Core везде)
- Консистентный UX (все команды в одном стиле)
- Проще документировать и тестировать

**Готов начать реализацию**: Жду подтверждения для начала интеграции функционала.
