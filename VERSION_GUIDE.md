# 🚀 Памятка: Обновление Версии CERES

## ⚡ Быстрый гайд перед каждым коммитом

### 1️⃣ Определи тип изменения

```
Баг-фикс       → PATCH версия (3.0.0 → 3.0.1)
Новая фича     → MINOR версия (3.0.0 → 3.1.0)
Breaking change → MAJOR версия (3.0.0 → 4.0.0)
```

### 2️⃣ Обнови версию (2 файла)

```bash
# 1. Обновить VERSION файл
echo "3.0.1" > VERSION

# 2. Обновить cmd/ceres/main.go
# Найти строку:
#   Version = "3.0.0"
# Заменить на:
#   Version = "3.0.1"
```

### 3️⃣ Проверь версию

```bash
# Проверка совпадения
cat VERSION
grep "Version = " cmd/ceres/main.go

# Должны совпадать!
```

### 4️⃣ Коммит с версией

```bash
git add -A
git commit -m "v3.0.1: Краткое описание изменений"
git tag v3.0.1
git push origin main --tags
```

---

## 📋 Checklist

- [ ] Определен тип изменения (MAJOR/MINOR/PATCH)
- [ ] Обновлен `VERSION` файл
- [ ] Обновлена переменная `Version` в `cmd/ceres/main.go`
- [ ] Версии совпадают (cat VERSION vs grep main.go)
- [ ] Коммит начинается с `vX.Y.Z:`
- [ ] Создан git tag `vX.Y.Z`
- [ ] Запушены теги `--tags`

---

## 🔧 Автоматизация (опционально)

### Скрипт для обновления версии

Создай `bump-version.sh`:

```bash
#!/bin/bash
NEW_VERSION=$1

if [ -z "$NEW_VERSION" ]; then
    echo "Usage: ./bump-version.sh 3.0.1"
    exit 1
fi

# Обновить VERSION
echo "$NEW_VERSION" > VERSION

# Обновить main.go
sed -i "s/Version = \".*\"/Version = \"$NEW_VERSION\"/" cmd/ceres/main.go

echo "✅ Версия обновлена до $NEW_VERSION"
echo "Проверь файлы:"
echo "  - VERSION: $(cat VERSION)"
echo "  - main.go: $(grep 'Version = ' cmd/ceres/main.go)"
```

Использование:
```bash
chmod +x bump-version.sh
./bump-version.sh 3.0.1
```

---

## 📊 Текущая версия

**v3.0.0** (2026-01-21)

Следующая версия:
- `v3.0.1` - если исправляешь баги
- `v3.1.0` - если добавляешь новые функции
- `v4.0.0` - если breaking changes

---

## ⚠️ НЕ ЗАБУДЬ!

**Перед каждым `git commit` - обнови версию!**

Иначе:
- ❌ Теги не будут соответствовать версии
- ❌ `ceres --version` покажет старую версию
- ❌ Сложно отследить изменения
