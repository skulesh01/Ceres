platform:
  name: CERES
  version: 3.0.0
  # Base domain used by services (keep in sync with /etc/ceres/ceres.env -> CERES_DOMAIN)
  # This file is a template: replace ${CERES_DOMAIN} with your real domain (e.g. company.com)
  domain: "${CERES_DOMAIN}"
  environment: production

cloud:
  provider: proxmox
  region: local
  project: ceres-platform
  
  # Proxmox connection
  proxmox:
    host: 192.168.1.3
    port: 8006
    user: root
    # Password stored in credentials.json

  # K3s cluster
  kubernetes:
    type: k3s
    endpoint: https://192.168.1.3:6443
    # kubeconfig: ~/.kube/config

services:
  # Core infrastructure
  postgresql:
    enabled: true
    version: "16"
    database: ceres
    storage: 10Gi
    password: R@nd0mP@ssw0rd!2025
  
  redis:
    enabled: true
    version: "7.0"
    storage: 5Gi
    password: ceres_redis_2025
  
  # Identity & Access
  keycloak:
    enabled: true
    replicas: 2
    admin_user: admin
    admin_password: K3yClo@k!2025
    domain: "auth.${CERES_DOMAIN}"
  
  # DevOps
  gitlab:
    enabled: false  # Large deployment, enable when needed
    domain: "gitlab.${CERES_DOMAIN}"
  
  nextcloud:
    enabled: false
    domain: "cloud.${CERES_DOMAIN}"
  
  # Monitoring
  prometheus:
    enabled: true
    storage: 20Gi
  
  grafana:
    enabled: true
    admin_user: admin
    admin_password: Grafana@Admin2025
    domain: "grafana.${CERES_DOMAIN}"
  
  # Network
  ingress-nginx:
    enabled: true
  
  cert-manager:
    enabled: true

namespaces:
  core: ceres-core
  apps: ceres
  monitoring: monitoring
