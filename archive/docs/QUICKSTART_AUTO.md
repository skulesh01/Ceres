# CERES - Автоматическое развертывание

## 🎯 Главное - теперь все автоматизировано!

Запустите **один скрипт** и получите готовый кластер:

```powershell
.\DEPLOY.ps1
```

## Что делает скрипт

### 1. Проверка и установка инструментов
- Скачивает `plink.exe` (PuTTY) для SSH с паролем
- Проверяет подключение к серверу

### 2. Установка k3s
- Настраивает DNS (Google DNS: 8.8.8.8, 8.8.4.4)
- Настраивает маршрутизацию (gateway через 192.168.1.1)
- Устанавливает k3s автоматически
- Проверяет, что кластер работает

### 3. Получение конфигурации
- Загружает kubeconfig с сервера
- Заменяет localhost на реальный IP
- Сохраняет в `%USERPROFILE%\k3s.yaml`

### 4. Подготовка секретов
- Генерирует SSH ключи (если нужно)
- Кодирует kubeconfig в base64
- Сохраняет все секреты в `%USERPROFILE%\ceres-secrets.txt`
- Пытается автоматически установить секреты в GitHub (если установлен gh CLI)

## Результат

После выполнения скрипта у вас будет:

✅ **k3s кластер** работает на 192.168.1.3  
✅ **kubeconfig** в `C:\Users\YourName\k3s.yaml`  
✅ **Секреты для GitHub** в `C:\Users\YourName\ceres-secrets.txt`  
✅ **SSH ключи** в `C:\Users\YourName\.ssh\ceres`  

## Следующий шаг - GitHub Actions

### Автоматически (если установлен GitHub CLI)

Скрипт попытается установить секреты автоматически. Проверьте:
```powershell
gh secret list -R skulesh01/Ceres
```

Должны быть установлены:
- KUBECONFIG
- SSH_PRIVATE_KEY
- DEPLOY_HOST
- DEPLOY_USER
- DEPLOY_PASSWORD

### Вручную (если gh не установлен)

1. Откройте https://github.com/skulesh01/Ceres/settings/secrets/actions
2. Нажмите "New repository secret"
3. Скопируйте каждый секрет из файла `ceres-secrets.txt`
4. Добавьте все 5 секретов

## Запуск деплоймента

После настройки секретов запустите деплоймент:

```powershell
gh workflow run deploy.yml -R skulesh01/Ceres
```

Или через веб-интерфейс:
https://github.com/skulesh01/Ceres/actions

## Параметры скрипта

Если у вас другие настройки сервера:

```powershell
.\DEPLOY.ps1 -ServerIP "192.168.1.10" -ServerUser "admin" -ServerPassword "yourpass"
```

## Проблемы и решения

### "Не удалось подключиться к серверу"
- Проверьте IP адрес: `ping 192.168.1.3`
- Проверьте пароль
- Убедитесь, что SSH работает: `ssh root@192.168.1.3`

### "k3s установлен но есть проблемы"
- Подождите 30 секунд и проверьте снова
- Зайдите на сервер: `ssh root@192.168.1.3`
- Проверьте статус: `systemctl status k3s`

### "GitHub CLI не аутентифицирован"
Выполните:
```powershell
gh auth login
```

Следуйте инструкциям на экране.

### Секреты не установились автоматически
Не проблема! Используйте файл `ceres-secrets.txt` для ручной настройки:
https://github.com/skulesh01/Ceres/settings/secrets/actions

## Что нового по сравнению со старыми инструкциями

### Старый способ (много шагов)
1. Установить SSH клиент
2. Настроить ключи
3. Подключиться к серверу
4. Вручную настроить DNS
5. Вручную настроить маршрутизацию
6. Скачать скрипт k3s
7. Установить k3s
8. Скопировать kubeconfig
9. Отредактировать kubeconfig
10. Закодировать в base64
11. Установить секреты в GitHub
12. Запустить деплоймент

### Новый способ (1 команда)
```powershell
.\DEPLOY.ps1
```

Все остальное - автоматически! 🎉

## Технические детали

Если интересно, как это работает, см:
- [LESSONS_LEARNED.md](LESSONS_LEARNED.md) - какие проблемы были решены
- [PROXMOX_NETWORK_CONFIG.md](PROXMOX_NETWORK_CONFIG.md) - настройка сети
- [HOW_TO_AVOID_PASSWORD_PROMPTS.md](HOW_TO_AVOID_PASSWORD_PROMPTS.md) - как работает plink.exe

## Поддержка

Если что-то не работает:
1. Проверьте, что Proxmox VM запущена
2. Проверьте сетевое подключение
3. Посмотрите лог скрипта - он покажет на каком этапе проблема
4. Откройте issue в GitHub с логом

## Резюме

**Раньше:** 2-3 часа ручной настройки  
**Сейчас:** 5 минут автоматической установки

Просто запустите `.\DEPLOY.ps1` и идите пить кофе ☕
