# 🌐 Proxmox Network Configuration - Как дать интернет VM

## Проблема
VM 192.168.1.3 не имеет доступа в Интернет (не может подключиться к get.k3s.io)

## Решение: Включить NAT на Proxmox

### Способ 1: Через Proxmox Web UI (графический)

1. Откройте Proxmox: https://192.168.1.1:8006
2. Слева выберите VM (например, ubuntu-vm или ceres)
3. Нажмите **Hardware** → **Network Device (net0)**
4. Проверьте что Bridge установлен правильно (обычно vmbr0)
5. Сохраните

Затем внутри VM выполните:
```bash
ip route add default via 192.168.1.1
```

### Способ 2: Через SSH на Proxmox хосте (консоль)

```bash
# Подключиться к Proxmox хосту
ssh root@192.168.1.1

# Проверить текущие iptables правила
iptables -t nat -L -n -v

# ВКЛЮЧИТЬ NAT (выполнить эту команду)
iptables -t nat -A POSTROUTING -s 192.168.1.0/24 -o vmbr0 -j MASQUERADE

# Проверить что правило добавилось
iptables -t nat -L -n -v

# Сохранить правила (они сохранятся после перезагрузки)
apt install -y iptables-persistent
netfilter-persistent save
```

### Способ 3: Отредактировать сетевой конфиг VM

Внутри VM 192.168.1.3 отредактируйте `/etc/network/interfaces`:

```bash
# Подключиться к VM
ssh root@192.168.1.3

# Отредактировать сеть
nano /etc/network/interfaces

# Добавить шлюз:
auto eth0
iface eth0 inet dhcp
  gateway 192.168.1.1
  # ИЛИ статический IP:
  # iface eth0 inet static
  #     address 192.168.1.3
  #     netmask 255.255.255.0
  #     gateway 192.168.1.1
  #     dns-nameservers 8.8.8.8 8.8.4.4

# Сохранить (Ctrl+X, Y, Enter)

# Перезагрузить сеть
systemctl restart networking

# Проверить доступ
ping 8.8.8.8
```

---

## ✅ Проверка работает ли интернет на VM

```bash
# Внутри VM 192.168.1.3:
ping -c 1 8.8.8.8

# Если работает:
curl -I https://get.k3s.io

# Если не работает - проверить маршруты:
ip route
ip -4 addr
```

---

## 🚀 После включения интернета

Просто повторите автоматизацию:

```powershell
# На Windows:
cd "E:\Новая папка\Ceres"

# Запустить установку k3s
& "$HOME\plink.exe" -pw "<DEPLOY_PASSWORD>" -batch root@192.168.1.3 "curl -fsSL https://get.k3s.io | sh -"
```

---

## 📋 Быстрая чек-лист

- [ ] Проверить Proxmox UI - включен ли Bridge для VM
- [ ] Добавить правило NAT на Proxmox хосте
- [ ] Проверить `ping 8.8.8.8` внутри VM
- [ ] Повторить k3s установку

---

**Дата:** 2026-01-01  
**Статус:** Нужна конфигурация сети Proxmox
