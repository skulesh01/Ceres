# 🔧 MANUAL INSTALLATION GUIDE
# ДЛЯ ВРУЧНОГО ВЫПОЛНЕНИЯ НА СЕРВЕРЕ 192.168.1.3

## ⚡ САМЫЙ БЫСТРЫЙ СПОСОБ

**Скопируйте и вставьте в консоль сервера:**

```bash
curl -fsSL https://get.k3s.io | sh -
```

**Это всё!** После этого будут установлены:
- ✅ Docker
- ✅ k3s (Kubernetes)
- ✅ kubectl

---

## 🖥️ Как подключиться к серверу

### Способ 1: Через SSH
```powershell
ssh root@192.168.1.3
# Пароль: !r0oT3dc
```

### Способ 2: Через Proxmox веб консоль
1. Откройте https://192.168.1.1:8006
2. Найдите VM 192.168.1.3
3. Нажмите **Console** (VNC)
4. Введите команду установки

---

## ✅ Проверка после установки

```bash
# Проверить версию k3s
k3s --version

# Проверить ноды
kubectl get nodes

# Проверить все pods
kubectl get pods -a

# Получить kubeconfig
cat /etc/rancher/k3s/k3s.yaml
```

---

## ⚙️ Если нужно переустановить

```bash
# Удалить k3s
/usr/local/bin/k3s-uninstall.sh

# Переустановить
curl -fsSL https://get.k3s.io | sh -
```

---

**Статус:** Ожидается ручное выполнение
# Введите пароль: !r0oT3dc
```

### На сервере (после подключения):

```bash
# 1. Обновить систему
sudo apt-get update
sudo apt-get upgrade -y

# 2. Установить k3s (выберите ОДИН из вариантов)

# Вариант A: Если есть интернет (get.k3s.io доступен)
curl -sfL https://get.k3s.io | sh -

# Вариант B: Если нет интернета - установить Docker + kubectl вручную
sudo apt-get install -y docker.io
sudo curl -L https://dl.k8s.io/v1.28.0/bin/linux/amd64/kubectl -o /usr/local/bin/kubectl
sudo chmod +x /usr/local/bin/kubectl

# 3. Проверить установку
k3s --version
kubectl get nodes
kubectl get pods -a

# 4. Получить kubeconfig
cat /etc/rancher/k3s/k3s.yaml
```

### Скопировать kubeconfig на Windows:

```powershell
# На Windows PowerShell:
scp root@192.168.1.3:/etc/rancher/k3s/k3s.yaml $HOME\k3s.yaml
```

---

## Или запустить установку одной командой:

### С интернетом:
```bash
curl -sfL https://get.k3s.io | sh -
```

### Без интернета (только Docker):
```bash
apt-get install -y docker.io
docker --version
```

---

## Если хотите автоматизировать - используйте это:

### Windows PowerShell (автоматическое выполнение):
```powershell
$plink = "$HOME\plink.exe"

# Убедимся что plink существует
if (-not (Test-Path $plink)) {
    Invoke-WebRequest -Uri "https://the.earth.li/~sgtatham/putty/latest/w64/plink.exe" `
        -OutFile $plink -UseBasicParsing
}

# Запустим установку (вводит пароль автоматически)
& $plink -pw "!r0oT3dc" root@192.168.1.3 "curl -sfL https://get.k3s.io | sh -"

# Ждём 60 секунд пока k3s инициализируется
Start-Sleep -Seconds 60

# Проверяем
& $plink -pw "!r0oT3dc" -batch root@192.168.1.3 "k3s --version"
```

---

## Checklist:

- [ ] k3s установлен: `k3s --version`
- [ ] kubectl работает: `kubectl get nodes`
- [ ] kubeconfig доступен: `/etc/rancher/k3s/k3s.yaml`
- [ ] Pods работают: `kubectl get pods -a`

---

**Помните:**
- SSH пароль: `!r0oT3dc`
- IP сервера: `192.168.1.3`
- Установка k3s занимает 5-10 минут
- После установки нужно дождаться инициализации (30-60 сек)
