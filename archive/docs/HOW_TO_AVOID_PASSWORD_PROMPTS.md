# 🎯 Инструкция: Автоматизация без интерактивных паролей

## Проблема
SSH на Windows требует вводить пароль каждый раз в интерактивном режиме. Невозможно автоматизировать.

## Решение: plink.exe
`plink.exe` из PuTTY позволяет **передавать пароль как аргумент**, избегая интерактивных подсказок.

---

## Как это работает?

### OpenSSH (стандарт Windows)
```powershell
ssh root@192.168.1.3 "command"
# ❌ Просит пароль интерактивно - нельзя автоматизировать
```

### plink (PuTTY)
```powershell
$plink = "$HOME\plink.exe"
& $plink -pw "!r0oT3dc" -batch root@192.168.1.3 "command"
# ✅ Пароль передан как аргумент - полная автоматизация!
```

---

## Пошаговая инструкция

### 1️⃣ Скачать plink.exe
```powershell
$plinkUrl = "https://the.earth.li/~sgtatham/putty/latest/w64/plink.exe"
$plinkPath = "$HOME\plink.exe"

Invoke-WebRequest -Uri $plinkUrl -OutFile $plinkPath -UseBasicParsing

Write-Host "✓ plink.exe готов в $plinkPath"
```

### 2️⃣ Тестировать подключение
```powershell
$plink = "$HOME\plink.exe"
$pass = "!r0oT3dc"

& $plink -pw $pass -batch root@192.168.1.3 "uname -a"
```

### 3️⃣ Создать SSH ключ (один раз)
```powershell
ssh-keygen -t ed25519 -f "$HOME\.ssh\ceres" -N ""

# Результат:
# ~/.ssh/ceres        - приватный ключ
# ~/.ssh/ceres.pub    - публичный ключ
```

### 4️⃣ Добавить публичный ключ на сервер (с plink)
```powershell
$plink = "$HOME\plink.exe"
$pass = "!r0oT3dc"
$pubKey = Get-Content "$HOME\.ssh\ceres.pub" -Raw

& $plink -pw $pass -batch root@192.168.1.3 "mkdir -p ~/.ssh"
& $plink -pw $pass -batch root@192.168.1.3 "echo '$pubKey' >> ~/.ssh/authorized_keys"
& $plink -pw $pass -batch root@192.168.1.3 "chmod 600 ~/.ssh/authorized_keys"
```

### 5️⃣ После регистрации ключа: используй SSH без пароля
```powershell
# Теперь работает без пароля:
ssh -i "$HOME\.ssh\ceres" root@192.168.1.3 "ls"

# Или если надо ещё пароль для чего-то:
& $plink -pw $pass root@192.168.1.3 "command"
```

---

## Примеры использования в скриптах

### Простой скрипт с plink
```powershell
param(
    [string]$Pass = "!r0oT3dc",
    [string]$Host = "192.168.1.3",
    [string]$User = "root"
)

$plink = "$HOME\plink.exe"

# Команда 1 - с паролем
& $plink -pw $Pass -batch $User@$Host "apt update"

# Команда 2 - с SSH ключом (после регистрации)
ssh -i "$HOME\.ssh\ceres" $User@$Host "docker ps"

# Команда 3 - SCP с ключом
scp -i "$HOME\.ssh\ceres" $User@$Host":/etc/hosts" ./hosts.txt
```

### Полная автоматизация
```powershell
$plink = "$HOME\plink.exe"
$sshKey = "$HOME\.ssh\ceres"
$pass = "!r0oT3dc"

# 1. Регистрировать ключ
$pub = Get-Content "$sshKey.pub" -Raw
& $plink -pw $pass -batch root@192.168.1.3 "echo '$pub' >> ~/.ssh/authorized_keys"

# 2. Запустить установку (с пароль если ещё нет ключа)
& $plink -pw $pass root@192.168.1.3 "curl ... | bash"

# 3. Дождаться и получить результат (с ключом)
Start-Sleep -Seconds 300
scp -i $sshKey root@192.168.1.3:/etc/rancher/k3s/k3s.yaml ./k3s.yaml
```

---

## Параметры plink

| Параметр | Описание |
|----------|---------|
| `-pw PASSWORD` | Пароль (вместо интерактивного ввода) |
| `-i KEY_FILE` | Файл приватного ключа |
| `-batch` | Не показывать интерактивные диалоги |
| `-o "KEY=VALUE"` | SSH опции (как в ssh) |
| `user@host` | Подключение |
| `"command"` | Команда для выполнения |

### Примеры
```powershell
# Базовое подключение с паролем
& $plink -pw "password" root@192.168.1.3 "echo OK"

# С ключом (если уже зарегистрирован)
& $plink -i "$HOME\.ssh\ceres" root@192.168.1.3 "echo OK"

# С опциями
& $plink -pw "pass" -o "StrictHostKeyChecking=no" root@192.168.1.3 "ls"

# Интерактивная оболочка
& $plink -pw "pass" root@192.168.1.3
# Теперь можно вводить команды как в обычном ssh
```

---

## ✅ Результаты

После настройки:

- ✅ **SSH-ключ** защищен, но не требует пароля для каждой команды
- ✅ **plink** позволяет передавать пароль автоматически
- ✅ **Скрипты** работают полностью без человеческого вмешательства
- ✅ **Безопасность** сохранена (пароль в переменных окружения/скриптах)

---

## Связанные файлы

- [QUICKSTART_PLINK.md](QUICKSTART_PLINK.md) - Быстрый старт
- [MANUAL_SETUP.md](MANUAL_SETUP.md) - Подробная установка
- `scripts/full-setup.ps1` - Полный автоматизированный скрипт
- `scripts/add-github-secrets.ps1` - Добавление GitHub secrets

---

**Создано:** 2026-01-01  
**Решение:** plink.exe из PuTTY для автоматизации без интерактивных подсказок
