# 🏗️ Сборка CERES v3.0.0

## Вариант 1: Сборка на Proxmox сервере

```powershell
# 1. Загрузить код на сервер
scp -r E:\Новая папка\All_project\Ceres root@192.168.1.3:/root/ceres-build

# 2. Собрать на сервере
ssh root@192.168.1.3 @"
cd /root/ceres-build
go build -o ceres cmd/ceres/main.go
"@

# 3. Запустить интерактивное меню
ssh root@192.168.1.3 @"
cd /root/ceres-build
./ceres
"@
```

## Вариант 2: Использование make

```powershell
ssh root@192.168.1.3 @"
cd /root/ceres-build
make build
./bin/ceres
"@
```

## Вариант 3: Прямое использование (без сборки)

```powershell
ssh root@192.168.1.3 @"
cd /root/ceres-build
go run cmd/ceres/main.go
"@
```

## 🚀 Быстрый Деплой

```powershell
# Загрузить код и развернуть одной командой
scp -r E:\Новая папка\All_project\Ceres root@192.168.1.3:/root/ceres-build
ssh root@192.168.1.3 @"
cd /root/ceres-build
go build -o ceres cmd/ceres/main.go
./ceres deploy
"@
```

## 📋 Интерактивное Меню

После сборки просто запустите:

```bash
./ceres
```

Появится меню с выбором действий:
- 1. Развернуть платформу
- 2. Показать статус
- 3. Исправить проблемы
- 4. Диагностика
- 5. Обновить платформу
- 6. Управление VPN
- 7. Конфигурация

## ✅ Проверка Работы

```bash
# Диагностика
./ceres diagnose

# Статус
./ceres status

# Исправление проблем
./ceres fix
```
